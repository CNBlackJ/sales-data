Then(/^我将看到"([^"]*)"月的月度目标为"([^"]*)",已完成"([^"]*)",完成率为"([^"]*)"$/) do |month, target, finished, percent|
  pending
end

And(/^看到以下图表$/) do |table|
  # table is a table.hashes.keys # => [:name, :redirect_url]
  pending
end

And(/^看到以下表格$/) do |table|
  # table is a table.hashes.keys # => [:项目, :月度目标, :完成率]
  pending
end